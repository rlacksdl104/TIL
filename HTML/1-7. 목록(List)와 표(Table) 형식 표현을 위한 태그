#1. 목록 list

1-1 . 순서없는 목록

<ul>와<li>로 만든다

1-2 . 순서있는 목록

<ol>와<li>로 만든다 (list는 주로 헤더의 메뉴바를 만드는 것에 사용이 됨)
-> type로 배열의 종류를 바꿀수있음
->  start : 리스트의 시작값을 지정할수있다
-> reserved : 리스트의 순서값을 역으로 바꾼다

#2. 표 table

2-1 . 표(table)를 만들 때 사용하는 태그이다.
과거에는 테이블 태그를 사용하여 레이아웃을 구성하기도 하였다
하지만 현대에 와서 웹에서는 주로 공간 분할 태그인 div 태그를 사용하여 레이아웃을 구성한다.

tr은 table row의 줄임말로 테이블의 가로를 담당한다
th는 table안에 들어가는 셀의 개수를 나타내며 이 안에 내용을 입력해서 

